# vim: ft=bash

# Path to the working tree of this repository
export CONFIG_REPO_PATH=$HOME/iresh-s-configs

# Source child rc files
for rc_file in $CONFIG_REPO_PATH/shell/{variables,functions.sh,aliases}; do
    source $rc_file
done

export EDITOR=vim
# set vi mode for easier editing
set -o vi
